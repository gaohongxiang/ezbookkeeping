# 个人财务实施规划

> 状态：当前实施、生产和任务唯一事实源
> 产品主线：`origin/main`
> 当前规划基线：PF v2 本轮账单同页流程发布实现 `f492aa61`（本规划发布记录提交为其后继）
> 重整前完整历史：[个人财务实施规划-20260821-重整前.md](./归档/个人财务实施规划-20260821-重整前.md)

本文只保存当前事实、稳定架构结论、活动任务、未决项和下一步。阶段一至 REFUND-1201 的详细契约、提交、测试、迁移、发布和回滚证据均保存在归档原文；普通会话不得加载全部历史。

## 0. 文档使用与当前看板

### 0.1 唯一事实源

- 根目录 `AGENTS.md`：长期协作、安全、分支、数据和验证硬约束；
- [个人财务用户需求.md](./个人财务用户需求.md)：已确认产品定位、范围、用户流程和验收；
- [个人财务业务规则与验收.md](./个人财务业务规则与验收.md)：会计语义和对错标准；
- [个人财务架构设计.md](./个人财务架构设计.md)：目标模块、状态、信息架构和迁移原则；
- [个人财务待处理问题与关系裁决设计.md](./个人财务待处理问题与关系裁决设计.md)：DESIGN-1701 候选，描述问题工作台、批量决定、同一事件、退款关系、服务端可入账校验和报表语义；用户验收前不视为冻结实现；
- 本文：当前生产状态、任务、未决项和下一步；
- `现行说明/`：当前代码实际行为；
- `归档/`：旧版原文与完成证据，只在追溯时读取。

任务看板由主负责人维护。执行会话只领取一个明确任务并报告待验收；只有集成、验证、文档和资源清理完成后才能标记已完成。

### 0.2 仓库与产品边界

- 本仓库是长期受控 fork，`origin/main` 是产品权威；
- `upstream/main` 只作为安全、数据损坏和数据库兼容修复来源；
- 正常部署保持一个 Go 模块化单体和 SQLite/MySQL/PostgreSQL 三选一；
- 现有 `Transaction`、`Account`、分类和标签是唯一正式账本；
- `personalfinance` 保存证据、解释、关系、债务计划和覆盖，核心账本不得反向依赖；
- 当前权威客户端是 Web/PWA，小程序保留在未来规划。

### 0.3 当前工程与生产基线

| 范围 | 当前事实 |
| --- | --- |
| 后端 | Go 1.26、Gin、XORM、模块化单体 |
| 前端 | Vue 3、TypeScript、Pinia、Vuetify/Framework7、Vite |
| 数据库 | SQLite、MySQL、PostgreSQL |
| PF schema | 正式站 v009，v001～v009 迁移均成功 |
| 正式镜像 | `ezbookkeeping-fork:2.0.0-pfv2-round-f492aa61` |
| 正式地址 | `http://localhost:8080` |
| 正式数据状态 | 3 份来源、308 条有效证据、36 条重复证据、272 个最终事件；138 个待入账、25 笔必须处理记录已按相同展示信息收拢为 23 组、109 个已排除；本轮未执行入账，正式交易仍为 7 条 |
| 主线 | PF v2 连续提交链已验收并进入 `origin/main`；当前发布实现为 `f492aa61` |
| 当前安全结论 | 现有 23 个问题组只是视觉分组，不能替代同一事件、退款、已有交易和批量决定的领域裁决；第一次真实入账继续暂停 |

生产配置、卷、备份目录和回滚容器只在运维文档与对应归档发布记录中维护，不复制到普通需求或现行说明。

### 0.4 状态定义

- `未开始`：未领取或依赖未满足；
- `进行中`：已有负责人、分支和工作区；
- `待验收`：候选已交付，等待复核或集成；
- `已完成`：已集成、按风险验证、更新文档并安全清理；
- `阻塞`：存在已记录且无法在授权内解决的外部依赖或用户决策。

### 0.5 当前任务看板

| ID | 目标 | 依赖 | 状态 | 负责人 / 分支 | 验收与边界 |
| --- | --- | --- | --- | --- | --- |
| DOCS-1301 | 重整产品需求事实源 | REFUND-1201 | 已完成 | 主负责人 / `main` | `30b43877`、`53bbdda7`、`9f5d30a6`；旧需求原样归档，用户确认结果优先但可核对 |
| DOCS-1401 | 重组全部活动文档和历史归档 | DOCS-1301 | 已完成 | 主负责人 / `codex/docs-1401-documentation-reset` | `9466ce1e`；旧业务规则、实施规划、运维和小程序提案已归档，新增索引、目标架构和三页现行说明；旧版哈希、链接、围栏和 diff-check 通过 |
| DESIGN-1501 | 冻结事件级整理、批次入账与语义解释设计 | DOCS-1401 | 已完成 | 当前主负责人 / `codex/docs-1401-documentation-reset` | `9466ce1e`；统一事件模型、批次原子入账、显式部分入账、退款并发、纠错、v009 重建和无旧 API 兼容方案已按发布实现验收 |
| PFV2-1601 | 建立 v009 新工作流表与仓储 | DESIGN-1501 | 已完成 | 当前主负责人 / `codex/pfv2-1601-organizer-schema` | `31840443`；update/event/relation/action 七表、隐私仓储和 v009 三库迁移已集成并在正式站升级成功；不提供旧工作流适配层 |
| PFV2-1602 | 实现统一经济事件整理引擎 | PFV2-1601 | 已完成 | 当前主负责人 / `codex/pfv2-1602-organizer-engine` | `77d9d903`、`ad7c4a05`；守恒计划、稳定身份、强跨来源证据、退款/还款/转账语义和可安全重整已集成；金额/时间相近不单独作为合并依据 |
| PFV2-1603 | 实现批次原子入账、纠错与撤销 | PFV2-1602 | 已完成 | 当前主负责人 / `codex/pfv2-1603-atomic-posting` | `8836c3a7`；原子整批/部分入账、安全撤销和字段级纠错已集成并通过阶段门禁 |
| PFV2-1604 | 切换资源化 API 并删除旧 billflow 路由 | PFV2-1603 | 已完成 | 当前主负责人 / `codex/pfv2-1604-resource-api` | `76a1826d`；updates/events/actions 已成为唯一资源 API，旧 billflow HTTP、启动装配和兼容字段已删除 |
| UX-1502 | 结果优先、按需核对的信息架构 | PFV2-1604 | 已完成 | 当前主负责人 / `codex/ux-1502-results-flow` | `48805155`；三步结果流、守恒摘要、必要问题、证据下钻和安全撤销已发布；旧五步页面和前端 billflow 层已删除 |
| PFV2-1605 | 工作数据重建、集成验收与发布 | UX-1502 | 已完成 | 当前主负责人 / `codex/pf-phase-v2` | `e3c25d4b`、`ad7c4a05`；唯一一次全仓/三库门禁、正式备份、v009 升级、工作投影重建、8080 切换、重启持久化和回滚对象保留均完成；未执行入账 |
| UX-1606 | 提高整理结果卡片信息密度 | PFV2-1605 | 已完成 | 当前主负责人 / `codex/pf-chinese-ui` | `a8eebab5`；列表直接展示商户、商品/备注、支付方式、账户、分类、来源数和原因，按页批量读取摘要；聚焦测试、构建、正式页和重启验收通过，未执行入账 |
| UX-1607 | 将上传、问题分组和入账重构为连续主流程 | UX-1606 | 已完成 | 当前主负责人 / `codex/pf-chinese-ui` | `ff6b495b`；上传入口回到主流程，25 笔必须处理记录按相同展示信息收拢为 23 组但不改变事件身份；主界面只突出账单份数、问题组数和可入账数，技术核对数字默认收起；聚焦测试、类型检查、范围 ESLint、构建、正式页和重启验收通过，不改 schema，不执行入账 |
| UX-1608 | 收紧单页信息密度并补齐交易账户核对 | UX-1607 | 已完成 | 当前主负责人 / `codex/pf-chinese-ui` | `c6dff029`、`9bad5578`；同一路由内切换「交易核对 / 账单记录」，后者只保留原始资料、解析和账户映射，不再逐行入账；整理弹窗补齐性质、记账账户、对方账户和分类；首屏、流程卡和事件行已压缩，账单记录双栏稳定并排；聚焦测试、类型检查、范围 ESLint、构建、正式页和重启验收通过，不改 schema，不执行入账 |
| UX-1609 | 将本轮账单改为同页上传与来源选择步骤 | UX-1608 | 已完成 | 当前主负责人 / `codex/pf-chinese-ui` | `ff9bd18d`、`f492aa61`；第一阶段只展开当前整理工作台，不再打开账单记录；新一轮可上传并勾选来源，已生成结果显示不可变来源快照；共用上传控件，不改 schema，不执行入账 |
| DESIGN-1701 | 冻结待处理问题、同一事件、退款关系和服务端可入账设计 | UX-1609 | 待验收 | 当前主负责人 / `codex/design-1701-review-issues` | 已形成候选设计；本任务只修改文档，不改代码/schema/生产；等待用户确认第 19 章冻结结论 |
| PFV2-1702 | 建立问题投影与统一可入账校验 | DESIGN-1701 | 未开始 | 主负责人待定 | 候选：稳定问题与成员、服务端派生 `ready`、批量动作原子性；schema 版本待 DESIGN-1701 冻结 |
| PFV2-1703 | 实现事件身份、已有交易和退款裁决 | PFV2-1702 | 未开始 | 主负责人待定 | 候选：同一笔/独立/无效证据/已有交易、跨历史退款候选和累计金额并发裁决 |
| UX-1704 | 实现问题工作台与批量决定 | PFV2-1703 | 未开始 | 主负责人待定 | 候选：替换前端视觉相似分组，按问题类型展示批量、对比和退款选择器 |
| DASH-1705 | 校准正式账本语义投影和报表 | PFV2-1703 | 未开始 | 主负责人待定 | 候选：退款不算收入、借款不算收入、偿还本金不算消费、余额调整口径明确 |
| PFV2-1706 | 生产副本集成与第一次真实入账验收 | UX-1704、DASH-1705 | 未开始 | 主负责人待定 | 候选：三库、正式副本、幂等、重启、总览和撤销通过后才允许首次真实入账 |
| INGEST-1503 | 输入覆盖与银行失败隔离 | DESIGN-1501 | 未开始 | 主负责人待定 | 支付宝/微信优先，未知银行形成缺口，不承诺万能 PDF |
| DEBT-1504 | 债务按需出现与分期非阻塞 | DESIGN-1501 | 未开始 | 主负责人待定 | 复用现有贷款能力，不重建合同系统 |

历史任务 DB-101、REC-201、LOAN-301、DASH-401、BILLFLOW-701、MERGE-901、MERGE-1001、BILLFLOW-1101、REFUND-1201 等均已完成；准确证据见归档原文，不在活动看板重复。

### 0.6 当前主负责人

- 当前阶段：DESIGN-1701 文档候选已交付，等待用户验收；用户确认前不启动实现任务；
- 主负责人：当前主会话；按根目录规则使用 `gpt-5.6-sol/high`，只有共享迁移、状态机总集成和正式发布才临时升级 `gpt-5.6-sol/max`；
- 当前分支：GitHub 文档分支 `codex/design-1701-review-issues`；本次通过 GitHub contents API 修改文档，未创建本地 worktree，不修改本地生产文件；
- 当前文件所有权：`docs/个人财务待处理问题与关系裁决设计.md`、`docs/README.md`、`docs/个人财务实施规划.md`、`docs/现行说明/账单整理与入账.md`；
- 当前正式站仍运行 `ezbookkeeping-fork:2.0.0-pfv2-round-f492aa61`，文档修改不会改变 8080 页面或接口；
- 当前没有活动代码实现会话；PFV2-1702～1706 均未开始；
- 用户原有未跟踪 `.workbuddy/` 与 `overview.md` 不属于本任务，不修改、不提交。

## 1. 已确认的产品方向

1. 产品是按周或按月更新的个人财务助手，不依赖每日手工记账；
2. 支付宝、微信是第一核心入口，银行文件是可选覆盖增强；
3. 默认先看结果，但自动处理必须可核对、纠错和撤销；
4. 清晰事件先完成，异常按文件、账户、身份或关系隔离；
5. 资金方向、经济性质、生活分类、关系和判断状态分开；
6. 贷款和分期保留但按需出现，不阻塞普通账单；
7. 核心正式账本不变，不引入第二套账本；
8. 小程序、CloudBase 和商业化暂缓，不永久关闭。

## 2. 当前系统已经具备

- 原文件、批次、原始行、来源账户和稳定来源身份；
- 支付宝、微信自动解析，通用银行 CSV 映射，光大信用卡 PDF 专用解析；
- 同文件防重、同来源精确去重、跨来源候选和人工决定；
- 原子 posting、证据下钻、幂等和三库兼容；
- 账户发现、沿用、忽略和单行不入账；
- 唯一交易计划、49 组跨渠道证据、明确退款和信用卡还款账户选择；
- 贷款/分期合同、计算、计划、实际结算和撤销；
- 财务总览、覆盖、信用卡账期和待还摘要。

这些能力优先复用或隐藏，不因主流程简化而删除。

## 3. 当前实现与目标需求差异

| 差异 | 当前行为 | 目标行为 |
| --- | --- | --- |
| 待处理主体 | 事件列表，前端按展示字段临时折叠相似记录 | 服务端稳定问题列表，一张卡片对应一个用户决定 |
| 重复判断 | 相似记录仍逐笔处理 | 多笔独立事件可原子批量应用同一答案，不改变事件身份 |
| 同一事件裁决 | 缺少同一笔、独立、无效证据和已有交易动作 | 显式裁决并持久化正向或负面身份事实 |
| 退款 | 主要在当前规划范围内自动候选，界面缺少完整历史原交易选择 | 跨当前更新、历史事件和正式交易搜索，确认关系后才能入账 |
| `ready` | 字段纠错和关系裁决边界未统一 | 由服务端统一可入账校验派生，客户端不能直接设置 |
| 报表语义 | 主要按核心交易类型推导 | 使用事件与关系语义，退款、借款、还款和余额调整口径明确 |
| 完成粒度 | 事件可整理、批次确认，但问题对象不稳定 | 事件级整理、问题级处理、默认整批原子入账 |
| 贷款入口 | 固定主导航 | 有债务或主动启用时突出 |
| 分期候选 | 可进入整理待办 | 非阻塞债务建议 |
| 银行文件 | 光大专用 PDF + 通用 CSV | 结构化优先、失败隔离、覆盖缺口 |
| 文档 | 已完成阶段和新缺口分散 | 当前、目标、候选设计、未来和归档分层 |

## 4. 共享设计硬约束

- 事件级状态、问题、批次入账、关系、迁移和 API 在实现前必须由主负责人串行冻结；
- 所有新用户表、查询和唯一约束带 `uid`；
- 只凭金额和时间不得自动合并；
- 批量应用同一答案不得改变独立事件身份；同一事件必须通过显式身份裁决或强稳定证据确定；
- 退款关系未确认、转账双方账户不完整或其他阻塞问题未解决时，不得进入 `ready`；
- 旧人工决定、已入账交易和原始证据不得被新规则静默覆盖；旧任务、todo 和自动候选允许备份后重建；
- 新 schema 只能使用连续 forward-only 显式迁移；
- 数据正确性同时在 SQLite、MySQL、PostgreSQL 成立；
- 账单工作流允许破坏性替换，不维护旧 API、页面和状态机适配层；仍须提供事实保留、备份和整库回滚方案；
- 原始账单和真实敏感夹具不进入提交、普通日志和聊天；
- DESIGN-1701 对应实现和生产副本验收完成前，不执行当前正式批次第一次真实入账。

## 5. DESIGN-1501 冻结结果

| 决策 | 结论 |
| --- | --- |
| 经济性质未知 | 持久 `EconomicEvent(needs_action)`，不进正式账本和报表 |
| 入账与幂等 | 默认整批原子入账；显式部分入账；更新版本、事件版本、动作键和请求摘要共同裁决 |
| 多次退款 | `refund_of` 关系累计不得超过原消费金额，关系版本和数据库条件更新处理并发 |
| 用户纠错 | 按字段锁定人工事实，自动规则只重算未锁定字段 |
| 数据保留 | 保留正式账本、原始证据、账户映射、来源身份和人工事实；旧任务、todo 和自动候选重建 |
| 总览 | 只统计正式交易；未入账和部分入账通过覆盖与批次状态明确展示 |
| 页面 | 结果摘要和守恒计数优先，事件、证据、字段来源和历史按需下钻 |
| 迁移与回滚 | v009 一次切换，无旧 API 兼容层；三库显式迁移，正式切换前备份，失败整库回滚 |

### 5.1 DESIGN-1701 候选结论

以下结论尚待用户验收，不得当作已实现事实：

| 决策 | 候选结论 |
| --- | --- |
| 待处理对象 | 新增稳定 `ReviewIssue` 与成员投影，问题组数与事件守恒分开统计 |
| 重复语义 | 多笔独立事件一次应用同一答案，精确成员版本原子提交，不自动合并 |
| 同一事件 | 提供同一笔、独立、无效证据和已有交易四类动作；同一笔最终为一个事件多条证据 |
| 退款 | 跨当前更新、历史事件和正式交易搜索候选；确认关系或外部原交易决定后才能入账 |
| `ready` | 客户端只提交事实和决定，统一服务端校验派生状态 |
| 报表 | 核心交易决定余额，事件和关系决定收入、消费、退款、借款、还款等展示语义 |
| 发布门禁 | PFV2-1702～1706 完成并通过生产副本演练后才执行第一次真实入账 |

详细候选见[个人财务待处理问题与关系裁决设计.md](./个人财务待处理问题与关系裁决设计.md)。

## 6. 验证策略

- 纯文档阶段只运行链接、哈希、行数、`git diff --check` 和范围差异审查；
- 设计冻结不运行应用测试；
- 普通实现任务只跑受影响包、必要的 race/vet 和 SQLite 聚焦测试，形成最小充分证据后立即停止；
- 只有 schema/唯一约束发生变化，或进入阶段集成、发布收口，才运行 SQLite/MySQL/PostgreSQL 门禁；同一迁移基线已通过后不得在后续单任务重复三库；
- 全仓、race、生产构建和正式副本演练只在阶段集成验收执行一次；
- 对用户可见的最终改动必须构建可追溯镜像并切换 8080，文档阶段不切服。

## 7. 发布与回滚当前指针

- 当前正式镜像：`ezbookkeeping-fork:2.0.0-pfv2-round-f492aa61`，镜像 ID `sha256:6469e1f4fabe344c9a974d781ec6497845cb1b65af655660f022fedfb53b792b`，OCI revision 为 `f492aa61d05b5301e35e34e4cdc1198b4c28da77`；
- 当前 schema：v009；正式副本 `quick_check=ok`，v001～v009 均成功；
- 本次一致性备份：`/Users/gaohongxiang/ezbookkeeping-production/backups/20260822-pfv2-dense-c6dff029-cutover`；数据库 SHA-256 为 `6ad91a3ed6c81a33a5c2512d8414c4beffaea12596fbcdaf24350a296b5f6194`，`quick_check=ok`；
- 回滚对象：来源明细修复前容器 `ezbookkeeping-pfv2-round-rollback-ff9bd18d`、本轮账单流程改造前容器 `ezbookkeeping-pfv2-round-rollback-9bad5578`、最终双栏修复前容器 `ezbookkeeping-pfv2-dense-rollback-c6dff029`、单页密度改造前容器 `ezbookkeeping-pfv2-dense-rollback-ff6b495b`、连续流程改造前容器 `ezbookkeeping-pfv2-flow-rollback-a8eebab5`、卡片优化前容器 `ezbookkeeping-pfv2-cn-b14f99ed`、中文化前容器 `ezbookkeeping-pfv2-ad7c4a05`、首版 PF v2 容器 `ezbookkeeping-pfv2-e3c25d4b` 与 v008 容器 `ezbookkeeping-refund1201-2ab3ec70` 均保留为停止态；v009 为 forward-only，若回退到 v008 程序必须同时恢复整库备份；
- 运维流程：[个人财务运维与备份.md](./个人财务运维与备份.md)；
- 本轮只重建未入账自动工作投影，没有新增、修改或删除正式交易。

## 8. PF v2 阶段完成证据

- 连续提交：设计与文档 `9466ce1e`，v009 schema `31840443`，整理引擎 `77d9d903`，原子入账 `8836c3a7`，资源 API `76a1826d`，结果流 `48805155`，集成说明与测试 `e3c25d4b`，生产预览修正 `ad7c4a05`；
- 阶段门禁：`go test ./...`、`go vet ./...`、前端 38,646 项全量测试、`vue-tsc --noEmit`、全量 ESLint、生产构建均通过；`scripts/test-pf-db.sh all` 在 SQLite、MySQL、PostgreSQL 通过。生产预览修正后只补跑受影响 organizer 包测试与 vet，没有重复全量和三库门禁；
- 正式结果：3 份来源、308 条有效证据、36 条重复证据、272 个最终事件、11 项事件关系；138 个 `ready`、25 个 `needs_action`、109 个 `excluded`、0 个 `posted`，证据链接 308、正式交易链接 0；
- 数据保留：正式用户/账户/交易/分类计数保持 `1/21/7/81`，原始证据、来源身份和账户映射保留；重启后版本 5 和上述计数不变；
- 页面验收：已登录正式页面完成重新整理和重启后复核，整批入账按钮因 25 个必要问题保持禁用，浏览器诊断无 error 日志；未点击入账。
- 中文界面修复：`b14f99ed` 将经济性质、来源类型和全部 organizer 原因码转换为国际化用户文案，未知内部码使用通用核对提示，原始商户和账单内容保持来源原文；organizer 聚焦测试 5 项、类型检查、范围 ESLint 和生产构建通过。正式页面复核未出现 `unknown`、`expense` 或下划线原因码，25/138/109 计数不变且浏览器无 error 日志。
- 卡片密度优化：`a8eebab5` 为事件分页响应批量投影主证据摘要，桌面卡片直接展示商户、商品/备注、支付方式、账户、分类、来源数和用户化原因，移动端复用摘要；无 schema 变化。受影响的 3 个 Go 包、14 个前端测试、TypeScript、定向 ESLint、生产构建和 `git diff --check` 通过，未重复全量或三库门禁。正式卡片宽度 994px 时行高约 99px，中部摘要列 329px，必须处理与待入账真实数据均显示完整摘要；重启前后 8080 为 200、25/138/109 计数不变、浏览器无 error 日志，未点击入账或处理。
- 连续流程与问题分组：`ff6b495b` 将上传账单、处理问题和一次入账放入同一主流程；25 笔必须处理记录按相同日期、金额、经济性质、账户、分类、展示摘要、来源数和原因码收拢为 23 个视觉问题组，其中 2 组各含 2 笔相似记录，展开后仍逐笔核对，不合并事件。主界面只保留 3 份账单、23 组问题和 138 笔可入账三个行动数字，272/109/0 降为次要摘要，证据守恒核对默认收起。3 个聚焦测试文件共 18 项、TypeScript、定向 ESLint、生产前端构建和 `git diff --check` 通过，未重复全量或三库门禁；正式页重启前后均为 200、无浏览器 error，整批入账按钮保持禁用，未点击入账或处理。
- 单页密度与账户核对：`c6dff029`、`9bad5578` 将桌面壳收紧为同一路由内的「交易核对 / 账单记录」视图，移除未接入 v2 整理引擎的旧重复/转账入口；账单记录负责原始文件、解析行和支付账户映射，删除逐行直接入账入口；必要问题弹窗统一核对交易性质、记账账户、对方账户和分类。3 个聚焦测试文件共 19 项、TypeScript、定向 ESLint、两次生产前端构建和 `git diff --check` 通过，双栏修复另复跑 4 项页面测试，未重复全量或三库门禁。正式页顶部 88px、流程卡 187px、事件行 80px，账单记录历史/明细在 1280px 下以 321/657px 并排；Tab 切换 URL 始终为 `/desktop#/personal-finance/bills`，重启后 HTTP 200、浏览器无 error，整批入账按钮保持禁用，未点击保存、排除或入账。
- 本轮账单同页流程：`ff9bd18d`、`f492aa61` 将第一阶段收回当前「交易核对」工作台；新一轮可在同页上传并勾选进入整理的账单，已有整理结果则展示并锁定本轮真实来源，新增上传只进入账单记录并留待下一轮，不会静默改变当前结果。「账单记录」继续只承担原始资料、解析和映射档案。首轮 3 个聚焦测试文件共 19 项，来源明细修复后复跑 2 个文件 13 项；TypeScript、定向 ESLint、生产前端构建和 `git diff --check` 均通过，未重复全量或三库门禁。正式页点击「3 份账单」后 URL 保持 `/desktop#/personal-finance/bills`，顶层仍选中「交易核对」，同页显示支付宝、微信和光大银行 3 份真实来源及上传入口，不出现「导入历史」；返回后恢复必要问题列表。容器重启前后 HTTP 200、浏览器无 error，数据计数仍为 3 份来源、23 组/25 笔必要问题、138 笔可入账、109 笔已排除、0 笔已入账，正式交易仍为 7 条；未点击上传、整理、保存、排除或入账。

## 19. 当前下一步

1. 用户审阅[个人财务待处理问题与关系裁决设计.md](./个人财务待处理问题与关系裁决设计.md)第 19 章的七项冻结结论，并提出确认或修改意见；
2. 用户确认前，DESIGN-1701 保持 `待验收`，PFV2-1702～1706 不启动，不修改代码、schema、正式容器或生产数据；
3. 不再要求用户按现有 23 个视觉问题组逐笔清空后直接入账；这些组继续作为当前页面事实保留，但不能替代同一事件、退款、已有交易和批量问题的领域裁决；
4. 用户确认后，先冻结需求、业务规则、schema、动作和迁移契约，再按 PFV2-1702 → PFV2-1703 → UX-1704 / DASH-1705 → PFV2-1706 的依赖实施；
5. 对应实现必须在生产副本验证批量决定、同一事件、历史退款、服务端 `ready`、幂等、重启、报表和撤销，并在需要时运行 SQLite/MySQL/PostgreSQL 门禁；
6. 只有 PFV2-1706 验收通过并再次获得用户明确授权后，才在正式站执行第一次真实“全部入账”；
7. 在核心闭环完成真实用户验证前，不新增银行 PDF、OCR、小程序或新债务类型。
